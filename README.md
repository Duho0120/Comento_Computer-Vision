# Comento_Computer-Vision
Comento Computer Vision Online BootCamp

## 1차 업무: 픽셀 단위 이미지 처리 (Pixel-level Image Processing)

이 프로젝트는 픽셀 단위로 이미지를 처리하는 기본적인 컴퓨터 비전 기능들을 구현합니다.

### 📋 주요 기능

- **이미지 입출력**: 이미지 파일 로드 및 저장
- **그레이스케일 변환**: RGB 이미지를 그레이스케일로 변환
- **밝기 조정**: 이미지의 밝기를 증가/감소
- **대비 조정**: 이미지의 대비를 증가/감소
- **색상 반전**: 이미지의 색상을 반전
- **이진화**: 임계값을 기준으로 이미지를 흑백으로 변환
- **이미지 자르기**: 특정 영역 추출
- **픽셀 값 읽기/쓰기**: 개별 픽셀 값 접근 및 수정
- **그라디언트 이미지 생성**: 테스트용 이미지 생성
- **이미지 통계**: 이미지의 최소/최대/평균/표준편차 계산

### 🛠️ 설치 방법

1. 저장소 클론:
```bash
git clone https://github.com/Duho0120/Comento_Computer-Vision.git
cd Comento_Computer-Vision
```

2. 필요한 패키지 설치:
```bash
pip install -r requirements.txt
```

### 📖 사용 방법

#### 예제 스크립트 실행

```bash
python example.py
```

이 스크립트는 다양한 이미지 처리 작업을 수행하고 결과를 파일로 저장합니다:
- `output_gradient_horizontal.png`: 수평 그라디언트 이미지
- `output_gradient_vertical.png`: 수직 그라디언트 이미지
- `output_test_color.png`: 테스트용 컬러 이미지
- `output_grayscale.png`: 그레이스케일 변환 결과
- `output_bright.png`: 밝기 증가 결과
- `output_dark.png`: 밝기 감소 결과
- `output_high_contrast.png`: 대비 증가 결과
- `output_low_contrast.png`: 대비 감소 결과
- `output_inverted.png`: 색상 반전 결과
- `output_binary.png`: 이진화 결과
- `output_cropped.png`: 자르기 결과
- `output_with_line.png`: 대각선이 추가된 이미지

#### 직접 사용하기

```python
import pixel_processing as pp
import numpy as np

# 이미지 로드
image = pp.load_image('input.png')

# 그레이스케일 변환
gray = pp.rgb_to_grayscale(image)

# 밝기 조정 (+50)
bright = pp.adjust_brightness(image, 50)

# 대비 조정 (1.5배)
contrast = pp.adjust_contrast(image, 1.5)

# 색상 반전
inverted = pp.invert_colors(image)

# 이진화 (임계값 128)
binary = pp.apply_threshold(gray, threshold=128)

# 이미지 저장
pp.save_image(gray, 'output_grayscale.png')
pp.save_image(bright, 'output_bright.png')

# 픽셀 값 읽기
pixel_value = pp.get_pixel_value(image, 100, 100)
print(f"(100, 100) 위치의 픽셀 값: {pixel_value}")

# 이미지 통계 정보
stats = pp.get_image_stats(image)
print(f"이미지 통계: {stats}")
```

### 📚 함수 설명

#### `load_image(filepath)`
- 이미지 파일을 NumPy 배열로 로드합니다.

#### `save_image(image, filepath)`
- NumPy 배열을 이미지 파일로 저장합니다.

#### `rgb_to_grayscale(image)`
- RGB 이미지를 그레이스케일로 변환합니다.
- 표준 가중치 사용: Y = 0.299*R + 0.587*G + 0.114*B

#### `adjust_brightness(image, value)`
- 이미지의 밝기를 조정합니다.
- `value`: -255 ~ 255 (양수는 밝게, 음수는 어둡게)

#### `adjust_contrast(image, factor)`
- 이미지의 대비를 조정합니다.
- `factor`: 0.0 ~ 3.0 (1.0이 원본, 1.0보다 크면 대비 증가)

#### `invert_colors(image)`
- 이미지의 색상을 반전시킵니다.

#### `apply_threshold(image, threshold)`
- 이미지에 이진화를 적용합니다.
- `threshold`: 0 ~ 255 (기본값 128)

#### `crop_image(image, x, y, width, height)`
- 이미지를 특정 영역만큼 자릅니다.

#### `get_pixel_value(image, x, y)`
- 특정 좌표의 픽셀 값을 반환합니다.

#### `set_pixel_value(image, x, y, value)`
- 특정 좌표의 픽셀 값을 설정합니다.

#### `create_gradient_image(width, height, horizontal)`
- 그라디언트 이미지를 생성합니다.

#### `get_image_stats(image)`
- 이미지의 통계 정보를 반환합니다.

### 🔍 구현 특징

- **픽셀 단위 처리**: 모든 함수는 픽셀을 직접 순회하며 처리합니다.
- **순수 Python**: OpenCV 없이 NumPy와 Python 기본 기능만 사용합니다.
- **교육 목적**: 컴퓨터 비전의 기본 원리를 이해하기 위한 명확한 구현입니다.

### 📝 요구사항

- Python 3.7 이상
- NumPy 1.21.0 이상
- Pillow 8.0.0 이상

### 🎯 향후 계획

- 2차 업무: 추가 예정
- 3차 업무: 추가 예정
- 4차 업무: 추가 예정
